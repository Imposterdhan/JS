/*
Function Hoisting
  -Functions are also hoisted before executing inside the js engine
named function 

1.in named fn the fn body will move to the top of the scope then the fn call will execute
  irrespective of whether the fn call is before the fn body or after the fn body

Function expression

 1.  It is also hoisted but expression call should be after the fn expression body






*/


