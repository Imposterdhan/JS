// FUNCTIONS

// Function are block of statements whenever it is called or invoked

// Applications of fn
// code resusability
// makes complex program into simple programs
// Note:There are _  of functions 


/*

1.  Anonymous function
2.  Named fn
3.  fn with expression
4.  Arrow fn
5.  IIFE (Immediate invoking fn expression )
6.  First class Function
7.  Callback Function
8.  higher oredr fn
9.  nested fn




Function syntax:

        Function a()             Function keyword  fn-name(Here we can pass the parameters)
        {                           {
                                              fn statement (fn statement will execute when fn is called)
             log(" xyz  ")          }
        }                          fn-name()
        a()


        Inside the fn call we can pass the arguments seperated by semicolon

1.Anonymous

 THE fn which does not have fn name is called as anonymous fn
 eg:
        Function()
        {
             log("Anonymous  Fn")
        } 
        ()
  
  2.Named fn

  A fn which has name is called named fn

     Function b()
     {
        log("Named fn")
     }
     b()



*/